{
  "name": "itschu.gg",
  "version": "1.0.0",
  "buildId": 4,
  "description": "itschu.gg static website",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && git add -A && git commit -m \"build: v$(node -p \\\"require('./package.json').buildId\\\")\" && git push"
  }
}
